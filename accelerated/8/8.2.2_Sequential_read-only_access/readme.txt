input iterator
An iterator that offers sequential read-only access to elements of a sequence should support ++ (both prefix and postfix),
==, !=, and unary *.
Also it should support iter->member and (*iter).member
