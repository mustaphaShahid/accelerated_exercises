#include <iostream>

using std::cout;
using std::endl;

enum Color { Red, Orange, Yellow, Green, Blue, Violet };

int main(int argc, char *argv[])
{
        int x{10};
        cout << x << endl;
        return 0;
}
